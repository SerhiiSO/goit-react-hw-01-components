import { createGlobalStyle } from "styled-components";

export const GlobalStyle = createGlobalStyle`
    body{
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        color: #000000;
        background-color:rgba(15, 209, 148, 0.4);

    }
    img{
        display: block;
        
    }
    ul{
        padding: 0;
        margin: 0;
        list-style: none;
    }

`;
